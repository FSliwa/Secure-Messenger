# ===============================================
# SUPABASE CONFIGURATION
# ===============================================
# Get these values from your Supabase project dashboard:
# https://app.supabase.com/project/[your-project-ref]

# Your Supabase project URL (format: https://xxxxx.supabase.co)
VITE_SUPABASE_URL=https://your-project-ref.supabase.co

# Your Supabase anon/public key (safe to expose in client-side code)
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.your-anon-key-here

# ===============================================
# APPLICATION CONFIGURATION
# ===============================================

# Base URL of your application (used for email redirects)
# Development:
VITE_APP_URL=http://localhost:5173

# Production (replace with your actual domain):
# VITE_APP_URL=https://your-domain.com

# Redirect URL for email confirmations and password resets
# Should match VITE_APP_URL in most cases
VITE_REDIRECT_URL=http://localhost:5173

# ===============================================
# ENVIRONMENT SPECIFIC SETTINGS
# ===============================================

# Current environment (development, staging, production)
VITE_ENVIRONMENT=development

# Enable debug logging (true/false)
VITE_DEBUG_MODE=true

# ===============================================
# SECURITY SETTINGS (Optional)
# ===============================================

# Session timeout in minutes (default: 60)
VITE_SESSION_TIMEOUT=60

# Maximum login attempts before lockout (default: 5)
VITE_MAX_LOGIN_ATTEMPTS=5

# ===============================================
# FEATURE FLAGS (Optional)
# ===============================================

# Enable/disable features
VITE_ENABLE_BIOMETRIC_AUTH=true
VITE_ENABLE_VOICE_MESSAGES=true
VITE_ENABLE_FILE_SHARING=true

# ===============================================
# SETUP INSTRUCTIONS
# ===============================================
#
# 1. Copy this file to .env (or .env.local for Vite projects)
# 2. Replace the placeholder values with your actual Supabase credentials
# 3. Configure Supabase Authentication:
#    - Dashboard → Authentication → Providers → Email: Enable
#    - Dashboard → Authentication → URL Configuration:
#      * Site URL: http://localhost:5173 (or your domain)
#      * Redirect URLs: Add all these URLs:
#        - http://localhost:5173/**
#        - http://localhost:5173/reset-password
#        - http://localhost:5173/auth/callback
#        - http://localhost:5173/dashboard
#        - (Add production URLs when deploying)
# 4. Check Email Templates in Supabase:
#    - Dashboard → Authentication → Email Templates
#    - Ensure templates contain {{ .ConfirmationURL }}
# 5. Restart your development server after making changes
#
# ===============================================
# TROUBLESHOOTING
# ===============================================
#
# If emails are not being sent:
# 1. Check Supabase → Logs → Auth Logs for errors
# 2. Verify email provider settings in Supabase dashboard
# 3. Check spam/junk folder in email client
# 4. Ensure Redirect URLs are properly configured
# 5. Verify environment variables are loaded (console.log in browser)
#
# Common issues:
# - Rate limiting: Supabase free tier limits email sending
# - Email confirmation required but not configured
# - Redirect URL mismatch between code and Supabase settings
# - Missing VITE_ prefix (required for Vite to expose env vars to client)
#